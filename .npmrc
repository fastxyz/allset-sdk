# permissionless@0.3.x declares `ox` as peerOptional@^0.11.3,
# but viem@2.46+ ships ox@0.14.x. The mismatch is benign at runtime
# (permissionless doesn't actually rely on ox APIs we care about),
# but npm@7+ treats peerOptional conflicts as errors without this flag.
legacy-peer-deps=true
