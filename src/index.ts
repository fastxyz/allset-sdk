/**
 * @fastxyz/allset-sdk — browser-safe core helpers
 *
 * Root exports are pure helpers only. Use `@fastxyz/allset-sdk/node` for
 * provider, executor, wallet, and file-backed config APIs.
 */

export * from './core/index.js';
